# The configuration of the module's own test, and it is nearly empty: there is no database to
# configure, which is what this blueprint is about. Where the BPMS is lives in the profile file
# of that engine, 'application-camunda8.yaml'.
logging:
  level:
    blueprint.workflowmodule: INFO
